pipeline {
    agent any

    stages {
        stage('List Files') {
            steps {
                echo "Listing files in workspace..."
                sh 'ls -al'
            }
        }
    }
}

